# حالة المشروع · Project Status

> **آخر تحديث:** 2026-07-20. اقرأ هذا الملف أولًا قبل أي عمل — يلخّص المُنجَز والمتبقّي بدقّة لكل وكيل/موديل يفتح هذا المستودع.

## السلسلة: ٦ كتب حول سطر الأوامر عبر الأنظمة الأربعة

| # | الكتاب | المسار | الحالة |
|---|--------|--------|--------|
| ١ | لينكس «مِن الصِّفر إلى الجَذر» | `books/1-linux/ar/` | ✅ **مكتمل تمامًا**: ٤٧ فصلًا + ٧ ملاحق، مدقّق عدائيًّا، يبني ٥٩٣ صفحة. مدفوعٌ على GitHub. |
| ٢ | ماك «ماك من الطرفية» | `books/2-macos/ar/` | ✅ **مكتمل تمامًا**: ١٩ فصلًا + ٥ ملاحق، مدقّق عدائيًّا (فروق BSD/GNU)، يبني ٢٢٩ صفحة. مدفوع. |
| ٣ | ويندوز «مِن الصِّفر إلى المسؤول» | `books/3-windows/ar/` | 🟡 **المحتوى مكتمل** (٢٢ فصلًا + ٥ ملاحق، ٣٠٤ صفحات، مدفوع)، لكن **التدقيق العدائيّ توقّف عند نصفه تقريبًا**. أُنجز: preface, how-to-read, ch01–ch06. **متبقٍّ للتدقيق:** ch07–ch22 + appA–appE (الملفّات نفسها سليمة تقنيًّا لأنها روجعت أثناء الكتابة، لكنها لم تخضع للتدقيق العدائيّ المستقلّ الأخير). سكربت الاستئناف: `wf_58dce796-f25`. |
| ٤ | BSD «مِن الصِّفر إلى العِفريت» | `books/4-bsd/ar/` | 🟡 **الهيكل والمقدّمات فقط** (٣٧ صفحة). **لا فصل واحد من الـ١٩ مكتوبٌ محتوًى فعليًّا** — كلّها جذاذات «قيدُ الكتابة». المولّد: `tools/gen_book4_bsd.py` (نُفّذ، الهيكل موجود). سكربت الكتابة (لم ينجح، يبدأ من الصفر): `wf_c36775ca-c5c`. ⚠️ **مسودّة `opencode/` (بأداة opencode/MiMo 2.5) مرفوضةٌ صراحةً** (2026-07-23) — تكسر البناء (استدعاء `#distro` بمعطيين خطأ)، فيها تلفٌ نصّيّ، وخطأ حقيقة (اسم مؤسّس OpenBSD)، وأقصر من المعيار. **لا تُستعمل**؛ يُكتب الكتاب من الصفر بخطّ الإنتاج المعتاد. |
| ٥ | دفتر التمارين | — | ⏳ **لم يبدأ إطلاقًا**. لا هيكل، لا مولّد، لا مجلّد `books/5-workbook/`. |
| ٦ | قصّة يونِكس (أدبيّ عن التاريخ/التطوّر/النواة) | — | ⏳ **لم يبدأ إطلاقًا**. لا هيكل، لا مجلّد `books/6-unix-story/`. |

## الأساس المشترك (جاهزٌ ومستقرّ)

- `lib/` — نظام التصميم المشترك لكل الكتب (ألوان، خطوط، القالب `book.typ`، الصناديق `components.typ`). **لا تُعدَّل إلا بحذرٍ شديد** لأنها تؤثّر على الكتب الأربعة كلّها دفعة واحدة.
- `docs/STYLE-GUIDE.md` — دليل الأسلوب الإلزاميّ لكل فصلٍ في أي كتاب (بنية الفصل، الصناديق، سياسة المصادر).
- كل كتابٍ فيه `main.typ` (نقطة البناء)، `frontmatter/` (عنوان، حقوق، مقدّمة، كيف تقرأ)، `chapters/`، `appendices/`.
- **البناء:** `cd ~/Desktop/zero-to-root && ~/.local/bin/typst compile books/N-name/ar/main.typ build/OUT.pdf --root .`

## الأغلفة (فنٌّ جاهز، لم يُدمَج بعد)

- **١٢ صورة نهائية** (٦ أماميّة + ٦ خلفيّة) بأسلوب «عاجيّ معتّق + مؤشّر متوهّج + جذور» — مولّدة ومحفوظة في:
  - `الأغلفة-الستة-النهائية/غلاف-0N-{color}-شامل-{hex}.png`
  - `الأغلفة-الخلفية-الستة-المصححة/غلاف-خلفي-0N-{color}-{hex}.png`
  - الترتيب: ٠١ طوبيّ=لينكس، ٠٢ أزرق=ماك، ٠٣ كهرمانيّ=ويندوز، ٠٤ عنّابيّ=BSD، ٠٥ بنفسجيّ=تمارين، ٠٦ نيليّ=قصّة.
- **سكربت معاينة معتمَد** لكتاب ١ (تخطيط النصّ فوق الصورة): `shared/covers/preview-script/preview-book1.typ` — **مستقلّ، لا يلمس ملفّات الكتاب الحقيقية**. القرارات المعتمدة من أبي هيثم: العنوان بسطرٍ واحد أعلى الصفحة (فوق الهالة، بلا تصادم مع `$`)، بلا خطّ فاصل تحته، المؤلّف بالأسفل؛ الغلاف الخلفيّ نصّه في المنتصف تقريبًا مع مسافة واسعة (`v(11mm)`) بين الفقرتين. **يجب ضبط `dir: rtl` صريحًا** في أي سكربت معاينة مستقلّ (الكتاب الحقيقيّ يضبطها صحيحة أصلًا عبر `lib/book.typ`).
- **لم يُدمَج أي غلافٍ في `main.typ` لأي كتاب حتى الآن.** المتبقّي: تكرار سكربت المعاينة لكل كتابٍ ٢–٦ بلونه، ثم موافقة أبي هيثم، ثم الدمج الفعليّ في الكتاب.

## قرارات ثابتة (لا تُغيَّر إلا بطلبٍ صريح جديد)

- **الموديل: Opus بالكامل** لكل الكتابة والتدقيق في هذا المشروع — نوقشت بدائل (مزج مع Sonnet لتوفير الحصّة) وقرّر أبو هيثم البقاء على Opus صراحةً (2026-07-20). لا تستعمل `model: "sonnet"` إلا بإذنٍ جديد.
- **النسخة الإنجليزية:** مؤجّلة، لم يُتّخذ قرارٌ بشأنها. لا تبدأ فيها تلقائيًّا.
- **المتطلّب الوحيد للقارئ في كل كتب السلسلة:** يعرف القراءة والكتابة — من الصفر المطلق دائمًا.
- **سياسة المصادر إلزامية:** كل كتابٍ يحتاج ملحق «المصادر والمراجع» (نصٌّ أصليّ + عزوٌ للمراجع الموثوقة). كتاب ١ فيه (appG)، ٢ فيه (appD)، ٣ فيه (appD)، ٤ الملحق موجودٌ هيكليًّا لكن بلا محتوى.
- **فصول جسريّة إلزامية بين الكتب:** كتاب ١ (لينكس) فيه فصلا BSD وماك؛ كتاب ٢ (ماك) فيه فصل جسريّ للينكس/BSD (ch19)؛ كتاب ٣ (ويندوز) فيه WSL كجسر؛ كتاب ٤ (BSD) يحتاج فصلًا جسريًّا للينكس (ch19 مخطَّطٌ في الهيكل، غير مكتوب).

## الترتيب المنطقيّ للاستكمال (عند العودة الخميس 2026-07-24، بإذنٍ صريح من أبي هيثم أولًا)

1. أكمل تدقيق كتاب ٣ ويندوز (استئناف `wf_58dce796-f25`) ← بناء ← دفع.
2. أطلق كتابة كتاب ٤ BSD من الصفر (١٩ فصلًا + ٥ ملاحق) ← تدقيق عدائيّ ← بناء ← دفع.
3. أنشئ هيكل ومحتوى كتاب ٥ (دفتر التمارين — يستمدّ من الكتب ١–٤).
4. أنشئ هيكل ومحتوى كتاب ٦ (قصّة يونِكس — أدبيّ، تاريخ/تطوّر/نواة، للفائدة العامّة).
5. أكمل تصميم الأغلفة للكتب ٢–٦ ودمجها في `main.typ` لكل كتاب.
6. (اختياريّ، لم يُقرَّر) النسخة الإنجليزية.

## للقراءة الآن

كتاب لينكس النهائيّ: `build/zero-to-root-ar.pdf` (أعد بناءه إن شككت أنه ليس آخر نسخة، بالأمر أعلاه).
