# حالة المشروع · Project Status

> **آخر تحديث:** 2026-07-23 (مساءً). اقرأ هذا الملفّ أولًا قبل أي عمل، ثم `docs/SERIES-SPINE.md`. يلخّصان المُنجَز والمتبقّي بدقّة لأي وكيلٍ يفتح هذا المستودع.

## السلسلة: 6 كتبٍ حول سطر الأوامر عبر الأنظمة الأربعة

| # | الكتاب | المسار | الحالة |
|---|--------|--------|--------|
| 1 | لينكس «مِن الصِّفر إلى الجَذر» | `books/1-linux/ar/` | ✅ **مكتمل**: 48 فصلًا + 7 ملاحق، مدقّق عدائيًّا، 599 صفحة. مدفوع. أُضيف إليه 2026-07-23 فصل «الطلاقة في الطرفية» (ch08) خاتمةً للجزء الأوّل، وأُعيد ترقيم ما بعده. **مرجع العمود الفقريّ — لا يُفتح إلا لثغراتٍ منصوصةٍ في SERIES-SPINE.** |
| 2 | ماك «ماك من الطرفية» | `books/2-macos/ar/` | 🟢 **مكتملٌ ومدقَّقٌ عدائيًّا، ينتظر مراجعة المدير ودمجه**: **47 فصلًا / 650 صفحة** على الأجزاء التسعة (كان 19 / 227). أُضيف 22 فصلًا، وسُدّ أكبر نقصين (النظام والمستخدمون، والأمن كاملًا). **دُقّق عدائيًّا** (2026-07-24، 23 فصلًا، 1023 أمر ماك نُفّذ فعلًا، 29 مشكلةً أُصلحت). ثمّ **قُسِمت ستّة فصولٍ طويلةٍ إلى اثني عشر مريحًا** بلا حذف مادّة (SSH، البيئات/الحاويات، مبادئ الأمن، الأمن المدمج، التعمية، التقوية/السجلّات). أطولُ فصلٍ الآن 30,499 محرفًا. الفرع `mb/book2-macos` مدفوعٌ للمراجعة. |
| 3 | ويندوز «مِن الصِّفر إلى المسؤول» | `books/3-windows/ar/` | 🟠 **أضعف الثلاثة**: 22 فصلًا / 306 صفحات. غائبٌ تمامًا: الصلاحيات وACL، والأمن، والجدار الناريّ، والجدولة. تقرّرت توسعته إلى 38 فصلًا (~480 صفحة). ✅ **التدقيق العدائيّ مكتملٌ تمامًا** (2026-07-23): 240 إصلاحًا في الملفّات الـ17 الأخيرة (ch11–ch22 + الملاحق) فوق 67 في ch07–ch10، والبناء 306 صفحات. |
| 4 | BSD «مِن الصِّفر إلى العِفريت» | `books/4-bsd/ar/` | ⏸️ **مؤجَّلٌ بقرارٍ صريح** (2026-07-23) حتى تُسوّى الكتب 2 و3 على العمود الفقريّ. الهيكل الحاليّ 19 فصلًا جذاذاتٍ فارغة ويُعاد توليده على 35 فصلًا. ⚠️ مسودّة `opencode/` (MiMo 2.5) **مرفوضةٌ صراحةً** — تكسر البناء، وفيها تلفٌ نصّيّ وخطأ حقيقة. لا تُستعمل. |
| 5 | دفتر التمارين | — | ⏳ لم يبدأ. **يعتمد على العمود الفقريّ**: لا يُكتب قبل تسوية 2 و3 و4. |
| 6 | قصّة يونِكس (أدبيّ) | — | ⏳ لم يبدأ. خارج العمود الفقريّ — كتابٌ للعامّة لا مرجع أوامر. |

## القرار المحوريّ الجديد (2026-07-23): العمود الفقريّ

اكتُشف تفاوتٌ بنيويٌّ بين الكتب سببه أن كتاب لينكس كُتب على تسعة أجزاء ثم كُتب التاليان على عمودٍ مضغوطٍ دون قرار. **`docs/SERIES-SPINE.md` هو الوثيقة المُلزِمة**: تسعة أجزاءٍ تُسقَط على الأنظمة الأربعة، مع قائمة الفصول المطلوبة لكل كتابٍ فصلًا فصلًا (الموجود ✓ والجديد ★). اقرأها قبل كتابة أي فصلٍ جديد.

## الأساس المشترك (جاهزٌ ومستقرّ)

- `lib/` — نظام التصميم المشترك (ألوان، خطوط، القالب `book.typ`، الصناديق `components.typ`). **لا تُعدَّل إلا بحذرٍ شديد** لأنها تؤثّر على الكتب كلّها دفعةً واحدة.
- ⚠️ **درسٌ مستفاد (2026-07-23):** كانت قاعدة الكود المضمَّن في `lib/book.typ` لا تضبط `dir: ltr`، فينقلب كلُّ أمرٍ داخل جملةٍ عربيّة في الطباعة (`cd ..` تُطبع `.. cd`، و`>` تنقلب مرآةً إلى `<`). عولجت في القالب، وأُزيلت 363 علامة اتّجاهٍ خفيّة (LRM) كانت مدسوسةً داخل الأوامر لعلاجه محلّيًّا — وضررها أنّ القارئ ينسخ الأمر فتُنسَخ معه فيرفضه سطر الأوامر. **لا تُدسّ علامات اتّجاهٍ داخل الكود مطلقًا.**
- `docs/STYLE-GUIDE.md` — دليل الأسلوب الإلزاميّ لكل فصل.
- `docs/SERIES-SPINE.md` — خريطة الفصول المطلوبة لكل كتاب.
- كل كتابٍ فيه `main.typ` و`frontmatter/` و`chapters/` و`appendices/` و`_contents.typ`.
- **البناء: `./build.sh` حصرًا** (أو `./build.sh 1-linux` لكتابٍ بعينه). يمرّر `--font-path fonts --ignore-system-fonts` فيعزل البناء عن خطوط النظام، ويقارن عدد الصفحات بالمرجع. **لا تبنِ بـ`typst compile` مجرّدًا** فتختلف النتيجة من جهازٍ إلى آخر.
- `fonts/` — خطوط المشروع مثبَّتةٌ داخل المستودع (13 ميغا، رخص OFL). أُضيفت 2026-07-23 بعد أن خرج كتاب لينكس 593 صفحة على الآيماك و595 على الماك، لأن رموزًا مثل `←` و`⌘` لا يغطّيها أيٌّ من خطوطنا فكان كلُّ نظامٍ يسدّها بخطٍّ من عنده. اقرأ `fonts/README.md`.

## الأغلفة (فنٌّ جاهز، لم يُدمَج بعد)

- 12 صورةً نهائيّة (6 أماميّة + 6 خلفيّة) بأسلوب «عاجيّ معتّق + مؤشّر متوهّج + جذور» في `الأغلفة-الستة-النهائية/` و`الأغلفة-الخلفية-الستة-المصححة/`. الترتيب: 01 طوبيّ=لينكس، 02 أزرق=ماك، 03 كهرمانيّ=ويندوز، 04 عنّابيّ=BSD، 05 بنفسجيّ=تمارين، 06 نيليّ=قصّة.
- سكربت معاينةٍ معتمَدٌ لكتاب 1: `shared/covers/preview-script/preview-book1.typ` — مستقلٌّ لا يلمس ملفّات الكتاب. **يجب ضبط `dir: rtl` صريحًا في أي سكربت معاينةٍ مستقلّ.**
- **لم يُدمَج أي غلافٍ في `main.typ` لأي كتاب.** المتبقّي: تكرار السكربت للكتب 2–6، ثم موافقة أبي هيثم، ثم الدمج.

## قرارات ثابتة (لا تُغيَّر إلا بطلبٍ صريح جديد)

- **الموديل: Opus بالكامل + نمط التفكير `max`.** الكتب 1–3 أُنتجت عليه، وهو الإعداد المرجعيّ. لا `sonnet`، ولا خفض النمط. مضبوطٌ في `~/.claude/settings.json`.
- **الأرقام: عربيّةٌ غباريّة `0123456789`** في النثر وترقيم الصفحات وأرقام الفصول — لا `٠١٢٣٤٥٦٧٨٩`. الملاحق تبقى بالحروف (أ، ب، ج) لأن الحروف ليست أرقامًا. ✅ **نُفِّذت كاملةً** (2026-07-23): القالب في مواضعه الثلاثة، و434 رقمًا في 52 ملفًّا، ودليل الأسلوب. لم يبقَ رقمٌ هنديٌّ واحدٌ في السلسلة، وتُحقّق منه بصريًّا.
- **المتطلّب الوحيد للقارئ:** يعرف القراءة والكتابة — من الصفر المطلق دائمًا. كلّ كتابٍ قائمٌ بذاته؛ الفصول الجسريّة إضافةٌ لا اعتماد.
- **الرخصة:** CC BY-ND 4.0.
- **سياسة المصادر إلزامية:** ملحق «المصادر والمراجع» في كل كتاب، نصٌّ أصليّ مع عزوٍ للمراجع.
- **النسخة الإنجليزية:** مؤجّلة، لا تبدأ فيها تلقائيًّا.

## الترتيب المنطقيّ للاستكمال

1. ✅ تدقيق كتاب 3 (ch11–ch22 + الملاحق) ← بناء ← دفع.
2. ✅ جولة الأرقام الغباريّة على القالب والكتب الثلاثة ← بناء ← دفع.
3. 🟡 كتاب 2 ماك: **كُتبت الفصول الـ22 وأُعيدت الهيكلة** (41 فصلًا / 637 صفحة). المتبقّي: (أ) قرار التقليم بعد عيّنة `ch12`، (ب) تنقية `ch17-sed-awk` وتعميقه، (ج) التدقيق العدائيّ، (د) الدفع.
4. وسّع كتاب 3 ويندوز: 16 فصلًا جديدًا ← تدقيق ← دفع.
5. أعد توليد هيكل كتاب 4 BSD على 35 فصلًا واكتبه ← تدقيق ← دفع.
6. كتاب 5 دفتر التمارين (يستمدّ من 1–4).
7. كتاب 6 قصّة يونِكس.
8. الأغلفة للكتب 2–6 ودمجها.

## خطّ الإنتاج المُثبَت

لكلّ فصل: **وكيل كتابة** (يقرأ دليل الأسلوب + فصلًا مرجعيًّا + مكتبة المكوّنات) ثم **وكيل مراجعة** (يعيد قراءة الدليل، ويبني الكتاب فعليًّا بـTypst، ويصلح). ثم — بعد اكتمال المحتوى — **تدقيقٌ عدائيّ** مستقلٌّ بوكيلٍ لكل ملفّ، مأمورٍ بافتراض وجود الخطأ، يفحص خمسة محاور: صحّة الأوامر والمخرجات (بالتنفيذ الفعليّ حيثما أمكن)، سلامة العربية، صحّة Typst، منهج «من الصفر»، الاتّساق. سكربتٌ جاهزٌ للتدقيق: `.claude/…/workflows/scripts/audit-book3-shard.js` (نسخةٌ منه على فلاش النقل).

**التوازي:** حدّ الوكلاء `min(16, الأنوية − 2)` لكل تشغيل. الآيماك 4 أنوية ← وكيلان. الماك i9 ذو 16 نواة ← 14 وكيلًا. ولو احتجت أكثر فقسّم العمل على عدّة تشغيلاتٍ متوازية، فالحدّ لكل تشغيلٍ لا للجهاز.

## للقراءة الآن

كتاب لينكس النهائيّ: `build/zero-to-root-ar.pdf` (أعد بناءه إن شككت أنه ليس آخر نسخة).
